id: example-ollama-worker
name: Example Ollama Worker
host: worker.example.internal
port: 8001
capabilities:
  - type: llm
    provider: ollama
    models:
      - example-model
    gpus:
      - GPU-0
